          #also can use serializer
            # serializer = TeamSerializer(results)
            # data = serializer.data
            # date = data['Date']
            # opponent = data['Opponent']
            # team = data["Team"]

            # if teamInstance is not None:
            #     date = teamInstance.Date
            #     opponent = teamInstance.Opponent
            #     print(opponent)
            #     team_name = teamInstance.Team
            #     print(team_name)
            #     print(date)

            #     opponentInstance = myModel.objects.filter(Opponent=opponent).order_by('-Date')[:10]

            #     serializer = TeamSerializer(opponentInstance)
            #     print("Test:", serializer.data)